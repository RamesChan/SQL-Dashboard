# import library
from bs4 import BeautifulSoup
import pandas as pd
import requests

# for Export the dataframe to collect at PostgreSQL
import psycopg2
# Importing the create_engine function
from sqlalchemy import create_engine

print("completed")
